# CLI platform

This platform reuses the Python package in `../../src/qq_mail_agent_cli`.
`start.ps1` launches the existing interactive terminal workflow without moving
or duplicating the shared core.
